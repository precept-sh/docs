{
  "name": "@precept.sh/docs",
  "version": "0.0.1",
  "scripts": {
    "dev": "hugo server --buildDrafts --buildFuture --buildExpired",
    "build": "hugo --gc --forceSyncStatic  --minify  --baseURL 'https://precept.sh/'",
    "publish": "gh-pages --dist public --nojekyll --cname precept.sh"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "gh-pages": "^6.1.1",
    "hugo-extended": "^0.136.2",
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0"
  },
  "packageManager": "yarn@4.4.1"
}